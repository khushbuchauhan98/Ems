
@*@model IEnumerable<leave_management.Models.AmountVM>*@
@model Data.FormModels.AmountRequestVM
@{
    ViewData["Title"] = "Index1";

}

<h1>Employees Salary</h1>

<p>
    <a asp-action="Create" asp-controller="Amount">Add New Employee Salary</a>
</p>
<table id="tblData" class="table">
    <thead>
        <tr>
            <th>
                Employee Name
            </th>
           
            <th>
               Salary
            </th>
            <th>
              Salary date
            </th>


            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.Amounts)
        {
                    <tr>
                        <td>
                    @Html.DisplayFor(modelItem => item.RequestingEmployee.Firstname) @Html.DisplayFor(modelItem => item.RequestingEmployee.Lastname)
                        </td>
                        
                       <td>
                    @Html.DisplayFor(modelItem => item.SalaryAmount)
                        </td>
                            <td>
                    @Html.DisplayFor(modelItem => item.Salarydate)
                            </td>
                @* <td>
                    @Html.DisplayFor(modelItem => item.RequestingEmployee)
                    </td>*@
                @*<td>
                    @Html.DisplayFor(modelItem => item.Createdby)
                    </td>*@
                @*<td>
                    @Html.DisplayFor(modelItem => item.CreatedbyId)
                    </td>*@
                            <td>
                       <div class="row">
                        @*<div class="col-2">
                           <a asp-action="Edit1" class="btn btn-warning" asp-route-id="@item.Id">
                                    <i class="fas fa-pencil-alt" aria-hidden="true"></i>Edit
                           </a>
                       </div>
                       <div class="col-2">
                           <a asp-action="Details" class="btn btn-info" asp-route-id="@item.Id">
                               <i class="fas fa-info-circle" aria-hidden="true"></i>Details
                           </a>
                       </div>*@
                           <div class="col-2">
                               <form asp-action="Delete" asp-route-id="@item.Id" method="post">
                                    <button type="submit" class="btn btn-danger" onclick="return confirm('Are you sure you want to delete this record?')">
                                            <i class="fas fa-trash" aria-hidden="true"></i>Delete
                                    </button>
                                </form>

                           </div>
                       </div>

                    </td>
                    </tr>
        }
    </tbody>
</table>
